spring:
  application:
    name: spring-cloud-zipkin-user
  zipkin:
    base-url: http://localhost:9411

server:
  port: 36002

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/